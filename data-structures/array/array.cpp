// equivalent to an ArrayList in JS
#include <iostream>
#include <stdexcept>

template <typename t>
class Array
{
private:
    T *data;
    size_t capacity;
    size_t length;
    void resize(size_t newCapacity)
    {
    }

public:
    Array()
    {
    }

    ~Array()
    {
    }
};

int main()
{
    /* code */
    return 0;
}
